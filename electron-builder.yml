extends: ./scripts/desktop/builder-channels.cjs
appId: com.suemor.Marchen
productName: Marchen
directories:
  buildResources: build
files:
  # 明确发布边界，避免把本地样片、旧 libav 构建缓存和规划资料打入应用。
  - 'out/main/**'
  - 'out/preload/**'
  - 'out/renderer/**'
  - 'resources/**'
  - package.json
  - '!**/*.map'
  - '!**/.vscode/*'
  - '!src/*'
  - '!resources/ffmpeg/**'
  - '!packages/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
win:
  executableName: Marchen
  fileAssociations:
    - ext: [mp4, m4v, mov, qt, mkv, mk3d, webm, ts, mts, m2ts, m2t]
      name: Video Files
      role: Editor
nsis:
  artifactName: ${productName}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  allowToChangeInstallationDirectory: true
  oneClick: false
mac:
  artifactName: ${productName}-${version}-${arch}.${ext}
  identity: '-'
  minimumSystemVersion: 13.0.0
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
  target:
    - target: dmg
      arch:
        - arm64
    - target: zip
      arch:
        - arm64
  fileAssociations:
    - ext: [mp4, m4v, mov, qt, mkv, mk3d, webm, ts, mts, m2ts, m2t]
      name: Video Files
      role: Editor
dmg:
  artifactName: ${productName}-${version}-${arch}.${ext}
npmRebuild: false
publish:
  provider: github
  owner: marchen-dev
  repo: marchen-player
# publish:
#   provider: generic
#   url: http://localhost:3000
afterPack: scripts/desktop/after-pack.cjs
