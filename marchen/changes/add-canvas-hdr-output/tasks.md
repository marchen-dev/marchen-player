> 本实现计划已由 [migrate-compat-video-presentation](../migrate-compat-video-presentation/proposal.md) 替代，勿继续执行 Canvas 主画面 HDR 接入。原任务和实验完成事实保留，不代表旧方案交付成功。

## 范围修订记录

2026-09-08 用户授权收敛为原本已走 libav 软解的视频的 HDR 输出。原 1.4/1.5/1.6 的硬解完整 HDR 及替代导入交付工作延期，不计为完成；试验记录仍保留于 evidence/hdr-import-spike.md。原 1.2/1.3 的已完成核查保留。原 3.1 WebCodecs HDR 接入撤下，改为软解数据传输；以下任务是当前交付范围，不再以硬解 HDR 突破为前提。

## 1. 已有证据、基线与软解小样

- [ ] 1.1 记录实际分支、版本、Mac 显示条件和代表样片；保存同后端 SDR 输出及 SDR 视频的短片段性能基线，不纳入无关工作区修改。
- [x] 1.2 核对合成 PQ 灰阶的编码、平面布局和数学参考，建立直接写入 FP16 的输出对照，明确参考白及目标编码下的预期值和容差，区分输入导入与输出阶段。
- [x] 1.3 核查当前 Chrome/Electron 导入色彩配置、tone mapping/headroom 接口和上游实现；通过行为差异确认参数生效，记录正式可用与实验开关路径，不能只看 API 不报错。
- [x] 1.4 打通 libav 实际 I420P10 输出到 HDR shader 的小样，验证原始平面未经过有损 VideoFrame 导入、相邻灰阶精度和 PQ 色块；明确参考白及量化容差。
- [ ] 1.5 在 Mac HDR 显示器上验证软解小样的实际高光与色彩对照，记录环境及测量限制；通过后接入正式呈现，失败则定位本软解路径而非继续研究硬解导入。

## 2. 渲染选择与 SDR 隔离

- [ ] 2.1 建立基于 libav 后端、原始高精度平面、PQ/HLG 元数据和显示能力的输出选择与会话缓存，覆盖 WebCodecs 保留 SDR、10bit SDR、元数据缺失和 Canvas 已禁用环境。
- [ ] 2.2 将软解 HDR 模块动态加载，SDR/H5/WebCodecs 保持现有入口；确认构建分块不会提前加载 HDR 专用代码或新增 GPU 资源。
- [ ] 2.3 在 context 创建前选择或替换画布，保持交互和覆盖层容器，绑定会话代次及帧关闭规则。

## 3. 高精度帧渲染

- [ ] 3.1 接入软解原始平面到 renderer 的受控传输，必要时在 VideoFrame 转换前分流，沿用背压和 seek 代次，确认不转移 WASM 所有内存；复用设备/缓冲，禁止每次 play/seek 重建。
- [ ] 3.2 完成 I420P10 软解平面上传及高精度色彩转换，覆盖 stride、range、matrix、primaries、visibleRect 和旋转，不增加 CPU RGBA 链路。
- [ ] 3.3 完成 PQ/HLG 参考白、传递函数和显示映射，明确静态元数据来源及缺失假设，验证灰阶、肤色、高光和黑边。

## 4. 回退、状态和生命周期

- [ ] 4.1 接入 HDR 不可用及初始化失败到现有 HDR→SDR 回退，保留解码后端；不能正确呈现时使用统一错误页。
- [ ] 4.2 处理显示条件变化、全屏和设备丢失的有界恢复，保留播放意图、音轨及时间，阻止旧会话异步帧覆盖。
- [ ] 4.3 在关闭及 HDR→SDR 切源时释放帧、纹理、缓冲、设备所有权和监听，验证重复切源资源有界。
- [ ] 4.4 扩展呈现状态、设置文案和现有遥测，报告真实输出及枚举回退原因，区分 WebCodecs 路径未支持 HDR 与显示器未支持；H5 保留浏览器管理状态，不新增用户开关或数据库字段。
- [ ] 4.5 确认字幕、弹幕与缩略图/历史封面维持原合成和 SDR 预览行为，检查 HDR 背景下的可读性。

## 5. 必要验证与交付记录

- [ ] 5.1 运行改动范围类型与必要静态检查；针对分类、异步失效和资源释放补充有意义的测试，不重复大量实现镜像测试。
- [ ] 5.2 验证 SDR 8/10bit、H5 和 WebCodecs HDR→SDR 无新增 HDR 模块/设备/高精度分配，且 HDR 不改变产品解码器选择；做短片段性能回归，按 design 的触发线定向复测异常。
- [ ] 5.3 在 Mac Electron 打包态及 Chromium 正式 Web 验证 PQ/HLG 软解 HDR 输出，以同一 libav 后端对比 HDR 和 SDR 输出并回归 WebCodecs/H5，记录约 60 秒性能、首帧及几次 seek，对缺少的样片/硬件明确标记待验证。
- [ ] 5.4 验证全屏、桌面切换、暂停恢复、播放中 seek、HDR→SDR 切源三轮、设备丢失和 SDR 回退；真实跨屏与模拟事件证据分开记录。
- [ ] 5.5 更新部署/支持说明并整理本变更验收材料，保留测光限制、未验证平台和剩余项，交由用户验收，不把任务完成等同于实机通过。
