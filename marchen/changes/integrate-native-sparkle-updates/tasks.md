## 1. ARM64 安装包原型门槛（完成后才进入正式接入）

- [x] 1.1 在 packages/sparkle-updater 建立最小 ARM64 N-API 桥接，锁定官方 Sparkle/工具版本和校验值，使用标准 UI；准备隔离的 bundle ID、数据目录、测试密钥和测试源，不修改正式更新通道
- [x] 1.2 产出两个 ad-hoc A/B 原型安装包，检查最终签名和 Framework/addon 布局，实测 A → B 全量安装重启与简繁中文窗口，记录版本和截图证据
- [x] 1.3 验证官方 delegate/退出机制能异步等待保存确认、处理超时与失败，以及后台提示延后和主动检查区别；记录具体 API 和实验结果，门槛失败则修订方案、不继续声称可发布

## 2. 工作区包与平台路由

- [x] 2.1 完善 loader、类型、主线程调度、生命周期释放及重复初始化保护，覆盖非 Mac、开发态、缺模块和配置错误；Windows/Web 安装不运行 Mac 编译
- [x] 2.2 接入主进程 UpdateService 平台路由与 IPC，Mac 停用 Squirrel 更新，保留 Windows electron-updater；原生加载失败提供可信下载页入口
- [x] 2.3 将关于页与 Mac 菜单接到标准窗口，配置语言资源、自动检查授权与偏好持久化，移除 Mac 旧更新 toast 和重复提示

## 3. 安装退出与 Windows 更新体验

- [x] 3.1 接通 renderer 历史立即写入和 requestId 回执，加入超时/失败保护、单次退出标记，协调窗口关闭和播放器释放，验证无活动媒体路径
- [x] 3.2 整理 Windows 更新状态、去重和带修订号的快照/订阅，显式关闭退出自动安装与开发真实更新，消费检查/下载失败
- [x] 3.3 更新 Windows 关于页与就绪轻提示，区分下载/准备/等待安装，支持用户下载、安装、失败重试；页面重开不丢状态
- [x] 3.4 用实际版本变化判定升级成功，绑定当前版本说明，去除点击安装即写成功标记的旧逻辑

## 4. 打包目标与最终资源

- [x] 4.1 正式 Mac ARM64 编译/打包接入 Framework、asarUnpack、公钥/feed 和许可证，保留完整媒体资源与 Electron 所需权限，完成最终修改后 ad-hoc 签名并停用旧 Apple 公证链
- [x] 4.2 删除 Linux/AppImage、Mac x64 目标及 build:linux、Linux 专属死代码；保留 Windows x64、通用 Ubuntu CI 和历史资料
- [x] 4.3 增加最终 DMG/ZIP/Windows 包资源、架构、签名和敏感文件检查，干净环境必须先执行 media:prepare，不以 out 检查替代包检查

## 5. 更新元数据与发布流程

- [x] 5.1 建立独立测试/正式 ARM64 appcast 配置，记录正式 Ed25519 公私钥生成、备份与 CI 配置方式，校验空值/占位公钥；不得提交或输出私钥
- [x] 5.2 使用官方工具生成全量及直接差分 appcast，筛选旧基线并保留正确下载地址，首发无基线全量；Windows 保留完整 latest.yml 与 blockmap
- [x] 5.3 改造 CI 的版本/tag/渠道校验、构建测试、原生工具环境和 contents:write，统一收集新产物至 draft，验证完整后发布；协调 Sentry 标记与幂等重跑
- [x] 5.4 验证旧 Mac/Intel 更新源不会指向 ARM64 新包、预发布不会污染正式 feed，提供发布失败撤回与更高版本修复流程

## 6. 正式验证与交付

- [x] 6.1 执行相关类型检查、更新服务/IPC/保存屏障测试和 Electron/Web 构建，覆盖重复请求、界面晚订阅、失败重试与实际版本判断
- [ ] 6.2 在 Mac 正式打包布局验证中文原生 UI、主动/后台检查、网络/权限错误、篡改与错误密钥拒绝；通过浏览器下载验证首次打开并记录 Gatekeeper 行为
- [ ] 6.3 在隔离的真实播放器包完成 A → B 全量及直接差分更新，覆盖无基线/差分失败恢复、播放与暂停时安装、保存超时失败、重启版本及历史进度，无残留声音
- [ ] 6.4 在 Windows x64 验证安装包更新、用户安装选择、退出不自动安装和差分/全量恢复；缺少环境时记录未验证，不勾选为通过
- [x] 6.5 更新 README、发布运行手册和迁移说明，覆盖 ARM64/Windows 范围、Linux/Intel 停更、首次手动迁移、旧记录不迁移及密钥应急处理
- [ ] 6.6 汇总逐项证据与发布前配置缺口，按 marchen-acceptance 提交验收；生产 secrets/正式发布未执行时明确区分本地验收与真实上线

## 7. 自动三渠道补充

- [x] 7.1 统一严格版本解析、排序、渠道范围及真实升级判断，设置 1.0.0-alpha.0 和发布说明
- [x] 7.2 按安装版本配置 Windows generic 源与 Mac 固定 feed，明确禁止降级，保留现有 UI
- [x] 7.3 支持 Alpha/Beta 打包、appcast、差分基线与资产校验
- [x] 7.4 构建仅创建 Draft，公开后原子重建渠道源，处理旧版本晚发布、缺资产、重跑及失败
- [x] 7.5 验证版本/渠道矩阵、元数据合成、现有主进程回归与构建，更新操作文档和证据；远端发布不执行
