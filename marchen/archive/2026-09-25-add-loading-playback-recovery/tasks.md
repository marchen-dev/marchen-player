## 1. 状态与恢复命令

- [x] 1.1 核对当前工作区的匹配 Toast/设置和加载改动，扩展保留视频的匹配失败态及 ready/reloading 恢复信息，区分跳过、失败和零条弹幕。
- [x] 1.2 增加匹配重试、失败后手动匹配和弹幕重试命令，明确允许状态、重试目标及重复点击互斥规则。

## 2. 加载与播放恢复

- [x] 2.1 扩展直接播放到匹配等待、弹幕等待、匹配失败和人工选择阶段，保留已导入视频及已确认匹配，默认加载仍等待弹幕完成。
- [x] 2.2 实现只从匹配步骤恢复，不重复导入或 hash；实现跳过已匹配弹幕后从设置主动加载。
- [x] 2.3 首次弹幕失败自动继续播放并保留本地及同匹配有效缓存，区分在线请求和本地存储错误。
- [x] 2.4 播放中重试和重新匹配保持 ready/reloading 的稳定视频快照；成功提交新数据，失败保留原数据及待重试目标，不销毁播放会话。

## 3. 请求隔离与持久化

- [x] 3.1 为加载和恢复操作增加代次及取消边界，取消或忽略迟到请求，覆盖直接播放、新 load、cancel、同 hash 重开；避免取消/失效请求产生错误提示。
- [x] 3.2 将请求结果和缓存/历史提交分离，加入有效性检查和同 hash 提交顺序，确保旧写入不会覆盖新操作。
- [x] 3.3 跳过路径保存正确持久化来源和匹配信息，保持本地来源及选择状态，避免失败写空弹幕覆盖已有记录；存储失败不阻断本来可播放的媒体。

## 4. 恢复界面

- [x] 4.1 在加载步骤说明下方添加“直接播放”按钮，无按钮下方说明文案；仅在视频导入完成后的匹配和弹幕等待阶段显示。
- [x] 4.2 匹配失败区域展示失败原因、“直接播放”主按钮、“重试匹配”和“手动匹配”；匹配弹窗原按钮改名并修正完成/关闭取消的语义。
- [x] 4.3 在现有 MatchResultToast 和 MatchInfo 添加正确目标的重试入口与进行中/失败状态；保留未匹配的手动匹配入口，提示消失后仍可从设置恢复。
- [x] 4.4 验证全屏容器、键盘操作、错误提示可感知性和默认箭头指针；不恢复顶部标题，不改字幕及其他无关界面。

## 5. 回归与验收证据

- [x] 5.1 用可控 Promise/请求测试覆盖匹配超时恢复、无结果选择、两种等待阶段直接播放、迟到成功/失败、重复点击和完成/跳过相邻竞争。
- [x] 5.2 测试本地与缓存弹幕保留、零条结果、匹配信息及持久化来源、存储失败、重试目标、新视频/同 hash 重开的代次和写入顺序。
- [x] 5.3 验证播放中恢复成功/失败保持媒体会话、进度和暂停状态；请求失败后旧弹幕仍可用，成功按当前时间接入，不补发过去弹幕。
- [x] 5.4 运行加载包和受影响运行时测试、pnpm typecheck、改动文件定向 lint 与 diff 检查，记录实际结果。
- [x] 5.5 使用受控 API 与真实本地视频完成桌面 Web 和 Electron 验证，记录慢请求直接播放、失败恢复、手动弹窗、全屏、Toast 消失后恢复、默认流程第 0/1 秒弹幕及当前时间接入证据；未完成的表面明确标注。
- [x] 5.6 按 marchen-apply 收尾要求执行 marchen-acceptance，生成可审阅的本地验收页，区分静态检查、自动回归和双端运行证据。

## 本轮实现与验证记录

- 加载包 33 项、播放运行时及相关服务 190 项测试通过；node/web 类型检查、定向 ESLint、Prettier 和 diff 检查通过。
- Web 和 Electron 各 17 项受控交互检查通过，另验证深色加载页与两端在当前时间接入弹幕。
- 本轮验收页已生成并启动，截图及报告位于 acceptance/rounds/1；用户已在验收页接受交付（accepted）；未代签核。
- Node 22.22.3 下执行，仓库声明 Node 24.x；未做发布构建、部署、Windows 实机验证或真实上游匹配准确率验收。
