## 1. 基线与迁移边界

- [x] 1.1 核对分支/WIP，整理已有硬解 HDR、停帧和用户确认的 WASM 可播放证据到本变更 evidence；区分未测的软解色准和长期同步，不重新阻塞于软解可播放性。
- [x] 1.2 审计主画面 Canvas、共享 HDR/预览模块、设置枚举及原生 video 引用；列出删除/保留清单，给 add-canvas-hdr-output 的 proposal/design/tasks 增加被本变更替代的交叉链接，保留原完成状态。

## 2. 兼容内核语义与唯一呈现器

- [x] 2.1 将兼容内核的类型/策略/资源边界统一为 compat，读取旧 canvas 偏好时归一化，保留 native/auto；同步校验与后续保存并测试旧设置。
- [x] 2.2 从原适配器分离唯一 VideoFramePresenter，保留 Worker、音频和时钟逻辑；接入 Chromium 轨道生成能力检测，不增加 Canvas 回退或用户呈现开关。
- [x] 2.3 实现帧所有权、最多一个待处理和一个在途写入、帧关闭及异常取消；用有意义的测试验证代次失效和恰好释放一次。
- [x] 2.4 实现首帧与定位帧的当前代次实际呈现确认、有限超时和拒绝播放处理；分别记录提交与呈现统计，不把 write 完成作为已显示。

## 3. 正式播放生命周期

- [x] 3.1 将停帧暂停与现有 AudioContext/媒体时钟冻结集成，兼容 video 不调用 pause；验证无音频、音频尾部和恢复时无重复排程。
- [x] 3.2 将轨道失效/重建纳入统一 seek 流程，覆盖所有调用入口；测试连续定位、旧请求迟到、暂停中目标帧显示以及恢复意图。
- [x] 3.3 接入倍速和换轨的统一定位/恢复，重复设置无操作；测试暂停中调整和播放中调整，避免重建后没有新帧或双重声音。
- [x] 3.4 实现 EOS、切源、退出和初始化失败的幂等释放，终止待处理 write/play/帧回调；测试错误后重试不复活旧会话。
- [x] 3.5 整合呈现失败与原生优先策略，移除 Canvas 导入失败强制软解逻辑；兼容不可用时明确错误/原生入口，验证有限重试且无切换循环。

## 4. 正式 UI 与周边接入

- [x] 4.1 替换 NativePlayer 的主画面 Canvas，建立原生/兼容独立 video 表面及可见性管理；原生事件不得改变兼容媒体状态，兼容表面固定静音。
- [x] 4.2 核对当前显示表面引用，接入字幕/弹幕的尺寸、旋转、全屏与统一时钟；回归暂停、seek、后台恢复、裁剪及像素宽高比。
- [x] 4.3 更新设置文案、能力提示及诊断/遥测，区分内核、解码后端、呈现、源 HDR 和浏览器管理状态；说明历史 canvas 事件与新 compat 语义的差别。
- [x] 4.4 删除主画面 Canvas renderer、专属 HDR shader/回退与失效测试，保留共享预览/字幕功能；历史实验如需旧实现则隔离到测试目录，不保留生产入口。

## 5. 针对性验证

- [x] 5.1 Node 24 下运行类型检查、改动范围静态检查及针对性生命周期/设置测试，完成 Electron/Web 构建，验证正式产物无主画面 Canvas 回退。
- [x] 5.2 正式 Electron 入口验证 HEVC 硬解、HEVC WASM、AV1、SDR 8/10-bit、PQ 和可取得的 HLG；复用选定样片，不把实验 WebM 读入当作新增产品支持。
- [ ] 5.3 在 Mac Electron 打包态和桌面 Chrome 上验证 HDR 高光、暂停保持和 SDR 映射；记录显示环境、移除强制 sRGB 自动化参数，标明不能证明的专业色准/位深事项。
- [ ] 5.4 使用明确同步点素材连续播放 10 分钟并覆盖倍速、暂停恢复、定位；区分可测偏移/漂移与人工口型观察，失败则定位显示队列与时钟，不用未经依据的固定补偿。
- [x] 5.5 同文件同解码后端记录约 60 秒 CPU/内存/可得 GPU、首帧/seek 与提交/呈现统计；验证三轮切源后资源有界，不承诺预估的性能收益。
- [ ] 5.6 回归快速连续拖动、暂停中换轨/倍速、全屏/显示变化、字幕弹幕、历史恢复、缩略图与原生内核切换；分别记录 Windows/Linux 可得环境结果，缺少条件则明确未验证而不勾选为通过。

## 6. 文档与验收

- [x] 6.1 更新架构、部署和支持说明，明确 compat 的唯一 video 路径、非标准接口边界、保留的 Canvas 用途及旧 HDR 方案被替代关系。
- [ ] 6.2 汇总正式入口证据及未验收项，完成本变更验收页并交用户签核；只生成规划/只跑实验不得标为实现完成，不把旧 Canvas HDR 任务伪造成已交付。

当前本机代码与可执行验证已完成；5.3 等待 HDR 人工观感，5.4 长测已运行但严格同步验收仍待复测瞬态抖动，5.6 缺少 Windows/Linux 与多显示器条件。未把这些项目勾为通过。
