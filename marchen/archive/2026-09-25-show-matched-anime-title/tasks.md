## 背景

匹配结果改用播放器右下角 shadcn Toast 提示，展示番名、集数和实际加载的匹配弹幕数量，约 5 秒自动消失。移除顶部标题，在设置 → 弹幕中常驻完整匹配信息和重新匹配入口。历史恢复不重复提示自动匹配，弹幕失败明确提示；弹幕数量只计匹配库已加载条目，不受显示开关影响，不包含另行导入的本地弹幕。覆盖桌面 Electron/Web，Toast 保持在播放器全屏元素内。

## 1. 匹配信息

- [x] 1.1 在加载状态中保留自动、历史和手动匹配来源及弹幕加载失败信息。

## 2. 匹配反馈

- [x] 2.1 移除顶部标题，复用 shadcn Toast 展示结果与数量，提供查看入口。
- [x] 2.2 在弹幕设置显示完整信息和重新匹配入口。

## 3. 验证

- [x] 3.1 检查计数、提示去重、历史恢复、失败与未匹配状态，以及设置入口、长文本和全屏可见性。

## 4. 弹幕快捷开关

- [x] 4.1 在控制栏设置按钮左侧增加弹幕开关，与设置双向同步，并支持 D 快捷键和无弹幕禁用提示。
- [x] 4.2 按用户最终选择使用 MingCute danmaku-on-line / danmaku-off-line，18px、同色、无常亮底色，移除自绘图标。

最终确认：2026-09-25 用户选择保留 MingCute 配对图标并请求归档。控制栏点击、快捷键、设置同步和无弹幕禁用的 9 项 Web 检查通过；最终图标的浏览器尺寸、透明背景及开关切换检查通过，定向 ESLint 通过。相关本地脚本与截图位于 `.tmp/danmaku-toggle/`。未创建正式验收签核页。

## 验证范围

本地 Web 验证使用视频样本和受控 API 返回，不证明上游匹配准确率。Electron 原生窗口未实测。本机 Node 22.22.3，包声明 Node 24，未据此宣称发布构建验证。

- `pnpm typecheck`、本次前端变更定向 ESLint、`git diff --check` 通过。
- 加载包 20 项回归通过。
- Chrome 真实 Web 页面验证 11 项交互：自动匹配及数量、顶部移除、右下位置、Web 全屏、查看设置、来源开关计数稳定及不重复通知、重新匹配入口、手动匹配更新、自动消失、历史恢复。
- 额外验证长文本、弹幕请求失败和未匹配文件名回退通过。
- `.tmp/matched-title/toast.png`、`settings.png` 为新方案截图；`toast-results.json`、`toast-edge-results.json` 为验证结果。旧的顶部标题截图不再代表当前实现。
