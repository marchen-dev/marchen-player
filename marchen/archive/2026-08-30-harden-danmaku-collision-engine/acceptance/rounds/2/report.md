# 第 2 轮报告

本轮新增持续时间、字号和显示区域三项设置连续更新：持续时间保留已有弹幕位置后平滑变速；字号和显示区域只影响后续弹幕，已有弹幕保持入轨时的样式与位置自然离屏。

应用内浏览器受本地地址访问策略限制，无法接管并截图当前验收页。本轮未改用其他浏览器控制方式规避限制，也没有复用第 1 轮截图冒充新行为证据，因此三个新增案例均标记为 `blocked`，等待维护者在现有桌面窗口手动确认。

当前 UI 没有独立的透明度、描边或颜色设置。本轮只在 Renderer 保留了非布局视觉配置的热更新边界，没有为不存在的控件编造验收案例。

按用户要求不执行原生全屏验证。
