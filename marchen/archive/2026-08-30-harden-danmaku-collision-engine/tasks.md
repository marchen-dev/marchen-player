## 1. 建立碰撞回归基线

- [x] 1.1 盘点 `DanmakuEngineCore`、轨道分配器、DOM Renderer、控制器遮挡上报和现有测试的状态流，记录旧节点与轨道状态可能分裂的所有入口
- [x] 1.2 为控制器矩形更新后复用旧轨道、hover 暂停后追击、顶部/底部取得相同 y 三个已确认问题补充失败回归测试
- [x] 1.3 扩展高密度验收样本，加入长短文本、集中时间点、滚动/顶部/底部混排和不同字号，并实现可复用的可见矩形相交判定辅助函数

## 2. 重建活动弹幕与统一轨道模型

- [x] 2.1 将弹幕测量契约从单一宽度扩展为宽高 metrics，定义活动弹幕生命周期、垂直起始轨道和跨轨数量的类型
- [x] 2.2 用活动弹幕注册表和统一垂直占用表替换滚动、顶部、底部三套独立数组，支持查询所有垂直跨度相交的活动记录
- [x] 2.3 实现顶部从上向下、底部从下向上、滚动弹幕按安全候选扫描的多轨分配，并在跨模式占用冲突时确定性丢弃
- [x] 2.4 重写滚动弹幕连续时间碰撞计算，覆盖完整入屏、水平间距、不同宽度/速度、追击时间、离屏释放和亚像素容差
- [x] 2.5 将固定弹幕持续占用和滚动弹幕可安全复用条件接入统一注册表，确保释放一个节点只移除它自己的全部轨道引用

## 3. 同步动画生命周期与容量

- [x] 3.1 为核心增加幂等的 pause、resume、complete/cancel 生命周期命令，按媒体时间累计单条暂停量并输出可测试的运动快照
- [x] 3.2 让 Renderer 在 hover 暂停/恢复和动画完成/取消时同步更新核心，再操作或回收 WAAPI 动画与 DOM 节点
- [x] 3.3 将最大在屏量改为依据活动注册表统计，验证 hover 暂停节点持续占用容量且节点池耗尽不会留下幽灵占用
- [x] 3.4 校准播放倍速变化时媒体时钟、核心运动进度和全部活动 WAAPI 动画的更新顺序，补齐播放、暂停、变速组合测试

## 4. 修复布局约束与原子重建

- [x] 4.1 将控制器遮挡更新改为只更新未来候选的排除轨道，不重置活动注册表，并对相同矩形上报去重
- [x] 4.2 保持控制器显隐、拖动和位置预设过程中的旧轨道占用，验证 `onDrag` 连续上报不会让新弹幕复用仍可见的轨道
- [x] 4.3 为 seek、数据替换和播放器 resize 建立统一 revision 重建事务，使旧动画/节点、活动注册表和轨道表原子清除
- [x] 4.4 处理字体 ready、字重和行高变化导致的测量缓存失效，并在无法确认字体尺寸时采用保守包围盒

## 5. 接入真实 DOM 尺寸的批量测量

- [x] 5.1 创建与最终弹幕样式一致的隐藏测量层，将候选节点按帧批量写入后统一读取实际矩形，再交给核心分配
- [x] 5.2 按文本与样式修订缓存测量结果，验证节点池复用、颜色变化和相同文本不会泄漏旧尺寸或旧样式
- [x] 5.3 调整候选调度与渲染边界，使测量、分配、挂载和 WAAPI 创建保持顺序一致，且 look-ahead 延迟期间节点仍正确定位
- [x] 5.4 清理不再使用的 Canvas 宽度估算路径，并检查是否存在直接改编 `danmu.js` 源码；如有则补齐 MIT 来源与许可说明

## 6. 完善无重叠与性能测试

- [x] 6.1 为不同播放器宽度、弹幕宽高、速度、间距和跨轨数量建立表格化纯算法测试，覆盖安全复用和必须拒绝的边界
- [x] 6.2 使用固定种子的密集弹幕序列逐时间采样预测矩形，断言滚动、顶部、底部及跨模式弹幕不存在不允许的相交
- [x] 6.3 增加 Renderer 组件测试，覆盖真实尺寸测量批次、hover 生命周期、控制器矩形更新、revision 重建、容量统计和节点池回收
- [x] 6.4 保留并扩展 seek、数据热更新、倍速、显示区域、显示开关和 resize 回归测试，确认修复碰撞不改变媒体时间语义
- [x] 6.5 为丢弃计数、在屏节点峰值和碰撞采样结果提供只读诊断入口，供验收记录正确性与密度权衡

## 7. 构建与桌面验收

- [x] 7.1 运行 `@marchen/danmaku-engine`、player-runtime 相关测试和项目 typecheck，修复本变更引入的问题
- [x] 7.2 运行 Electron 与 Web 构建，确认 workspace 类型、Renderer 测量层和生产打包均正常
- [x] 7.3 在 Web 桌面窗口运行高密度样本，验证正常播放、倍速、hover、控制器显隐/拖动和窗口 resize，自动采样可见矩形并记录零非法相交
- [x] 7.4 在用户允许操作电脑时于 Electron 窗口态执行同一套碰撞回归；原生全屏和 Web DOM 全屏不作为本变更验收门槛（本轮电脑占用，按条件豁免）
- [x] 7.5 对比变更前后的节点峰值、丢弃量、Long Task 和主观密度；若布局测量成本过高，优先收敛缓存与批量读写，不降低无重叠保证
- [x] 7.6 全仓搜索确认没有重新引入 xgplayer 或 `danmu.js` 运行时依赖，并复核实现范围未覆盖当前工作区中的无关播放器设置改动

## 8. 设置调整保持弹幕连续

- [x] 8.1 为活动弹幕增加持续时间运动重基准，在保留当前位置和暂停状态的同时同步核心进度与 WAAPI timing
- [x] 8.2 将字号调整改为只影响后续弹幕，使用稳定虚拟轨道基准和每条活动记录的入轨尺寸支持新旧字号安全共存
- [x] 8.3 将显示区域调整改为只约束后续候选，保留已有弹幕位置和占用直到自然离屏
- [x] 8.4 将不改变节点矩形的样式改为活动节点热更新，不重建运动或轨道状态
- [x] 8.5 增加 Renderer 与核心回归测试，覆盖设置调整不清屏、变速不跳位、暂停状态保持、新旧字号共存和显示区域渐进生效
- [x] 8.6 运行弹幕引擎测试、player-runtime 测试、typecheck 与 Web 构建，并在桌面窗口验收连续调整设置时无非法相交
