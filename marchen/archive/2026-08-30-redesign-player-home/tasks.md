## 1. 首页数据模型与选择规则

- [x] 1.1 在 `page/player/home/` 定义首页视图模型和纯选择器，完成 updatedAt 排序、可执行记录过滤、0%–90% 续播条件、Hero 去重、最近播放最多 4 条以及百分比 clamp
- [x] 1.2 为选择器补充单元测试，覆盖 0%、恰好 90%、异常 duration/progress、异常日期、无 path、Hero 缺失、超过 4 条和重复 hash
- [x] 1.3 实现 Electron 首页数据 hook：订阅 HISTORY，选出最多 5 个展示项，并按 animeId 批量读取 library 作为标题和封面的降级补充
- [x] 1.4 在数据边界实现 Web 降级，确保 Web 不订阅或输出依赖持久化本地路径的续播内容

## 2. 复用历史加载链路

- [x] 2.1 抽取“根据 hash 读取 HISTORY 并调用 `PlayerLoadingService.loadFromPath`”的 renderer 共享动作，定义记录缺失、path 缺失和加载失败的结果
- [x] 2.2 将 `useLoadingHistoricalAnime` 改为调用共享动作，保持路由 state 被消费和清除且刷新不重复加载
- [x] 2.3 为共享动作及路由接入补测试，覆盖 library 跳转、播放器首页直接续播、缺失记录、不可访问文件反馈和单次加载

## 3. 启动台组件

- [x] 3.1 创建 `PlayerHome` 容器和首次使用空状态，在没有历史内容时只展示居中的导入入口与 MP4/MKV、自动匹配弹幕说明
- [x] 3.2 创建 `ImportCard`，复用 Electron 文件对话框与 Web file input，并使用有可见文字、可键盘触发的原生交互元素
- [x] 3.3 创建 `ResumeHero`，展示 thumbnail → cover → 中性占位的图片降级、作品/集数、文字百分比、进度条和继续播放操作
- [x] 3.4 创建 `RecentGrid`，最多展示 4 张非 Hero 的最近播放卡，并在 Electron 提供进入影视库的轻量入口
- [x] 3.5 创建 `DragOverlay` 和稳定的 dragenter/dragleave 状态管理，在 drop、离开根区域和卸载时正确清理，同时保留现有格式校验与错误 toast
- [x] 3.6 在播放器页面用启动台替换 `DragTips`，验证 idle、loading、waiting_user、error、ready 和 reloading 分支不会互相覆盖

## 4. 轻量影院视觉与可访问性

- [x] 4.1 新增 scoped `player-home.css` 并接入样式入口，实现约 1180px 内容宽度、Hero/导入卡双列、最近播放 Grid 和窄桌面窗口单列降级
- [x] 4.2 使用现有语义 token 完成浅色与深色主题，建立 14–20px 圆角、轻边框、图片渐变和高对比 CTA，不引入新的全局品牌色
- [x] 4.3 实现 160–220ms 的克制进入/悬停反馈，并在 `prefers-reduced-motion` 下移除装饰性缩放、位移和图片 zoom
- [x] 4.4 核对交互语义、键盘顺序、可见焦点、图片 alt、装饰图标隐藏和进度文字，确保拖拽不是唯一导入方式且交互元素保持系统箭头指针

## 5. 回归与运行时验收

- [x] 5.1 扩充 player UI regression 测试，覆盖首次使用、Electron 有 Hero/只有 recent、Web 隐藏历史、首页开始加载后退出和 reduced motion
- [x] 5.2 运行相关单元测试、`pnpm typecheck` 和 `pnpm lint`，修复本变更引入的问题
- [x] 5.3 在 Electron 分别验收浅色/深色、空历史/有历史、关键帧/封面/无图、拖入/拖离、Hero/最近播放点击和历史文件失效，并保存截图证据
- [x] 5.4 在 Web 验收导入优先空状态、文件选择、拖拽、格式错误反馈以及不存在不可执行历史入口，并保存截图证据

## 6. 第 1 轮反馈后的扁平化重做

- [x] 6.1 将首页背景、Hero、导入区和最近播放改为细边界优先的扁平层级，移除径向背景与普通卡片投影
- [x] 6.2 为 Web 空态提供无悬浮卡片的播放器舞台视觉，同时保持 Electron 空历史和有历史布局一致可用
- [x] 6.3 收敛圆角、按钮和拖入覆盖层，确保夜间主题与 reduced motion 不退化
- [x] 6.4 运行定向回归并重新进行 Electron/Web 可见验收，保存第 2 轮截图证据

## 7. 第 2 轮反馈后的简约分格重做

- [x] 7.1 将首页最近播放数据收敛为最多 2 条，并同步选择器测试和 library 补充范围
- [x] 7.2 重构续播、导入和最近播放组件为低信息密度分格内容，保持原生交互语义
- [x] 7.3 实现统一外框、内部 1px 分隔线和仅外层轻微阴影，并为 Web 提供左右两格布局
- [x] 7.4 运行定向回归并重新进行 Electron/Web 可见验收，保存第 3 轮截图证据

## 8. 第 3 轮打回后的极简空态

- [x] 8.1 移除播放器首页的 HISTORY 查询、续播 Hero、最近播放和影视库入口，只保留文件导入能力
- [x] 8.2 完整恢复旧版线框播放图标与“点击或拖拽动漫到此处播放”，去除新增按钮、卡片、分格和阴影
- [x] 8.3 恢复旧版 Electron/Web 点击选择与整页 drop/dragover，保留格式错误反馈和原有动效
- [x] 8.4 更新定向回归并完成 Electron/Web 浅色与夜间可见验收，保存第 4 轮截图证据

## 9. 原版空态上的通用拖拽反馈

- [x] 9.1 新增通用 `VideoDropZone` 与覆盖层，稳定处理文件拖入深度、离开、释放、禁用和卸载清理
- [x] 9.2 播放器接入通用拖拽区域，保持原版 `DragTips` 常驻视觉及现有点击、格式校验和加载行为
- [x] 9.3 影视库接入通用拖拽区域，支持 MP4/MKV 释放后进入播放器，并在详情弹层打开时禁用
- [x] 9.4 补充定向回归并完成播放器/影视库拖入状态可见验收，保存第 5 轮截图证据

## 10. 拖拽提示文案收敛

- [x] 10.1 从通用拖拽覆盖层移除“支持 MP4、MKV”，并验证播放器与影视库只显示主提示
