# Verification

- 相关 Vitest：4 个测试文件，共 27 项通过。
- `pnpm typecheck`：通过；环境提示当前 Node 22，项目声明 Node 24。
- 本变更定向 ESLint：通过。
- `pnpm lint`：未全量通过，阻塞来自相邻工作区改动中的 `packages/danmaku-engine/src/index.ts` 导出排序和 `player-ui-regressions.test.ts` 测试标题规则；未越界修改这些文件。
- Electron：真实开发窗口验证有历史启动台并保存截图。
- Web：真实本地页面验证空态、MP4/MKV input accept、浅色/夜间主题并保存截图；完成后恢复系统主题。

## 第 1 轮反馈后的重做

- 移除首页径向背景、Hero/导入区/最近播放和按钮的普通卡片投影，改为纯色背景与细边界分层。
- Web 空态改为 16:9 扁平播放器舞台，不再使用孤立的悬浮导入卡。
- 定向 Vitest：3 个测试文件，共 15 项通过。
- `pnpm typecheck` 与本次修改文件的定向 ESLint：通过。
- Electron 有历史首页、Web 浅色和 Web 夜间主题均重新取证；验收后恢复系统主题。

## 第 2 轮反馈后的简约分格重做

- Electron 首页收敛为一个统一外框，通过内部 1px 分隔线组织续播、导入和最近播放；只在外框保留轻微阴影。
- 最近播放由最多 4 条收敛为最多 2 条，续播区整块可点击，最近卡片仅保留作品与集数。
- Web 移除无效侧栏，将设置入口移至标题栏；导入空态改为左右两格，不再模拟宽大的播放器舞台。
- 定向 Vitest：3 个测试文件，共 15 项通过。
- `pnpm typecheck` 与本次修改文件的定向 ESLint：通过；环境仍提示当前 Node 22，项目声明 Node 24。
- Electron 有历史首页、Web 浅色和 Web 夜间主题均重新取证；验收后恢复系统主题。
- 转码变更的兼容边界已核对：首页继续只消费持久化的 hash、path 与逻辑进度，不保存或展示临时转码 URL/session。

## 第 3 轮打回后的原版空态恢复

- 完整恢复原版 `DragTips`：线框播放图标与“点击或拖拽动漫到此处播放”，整块提示区域响应点击。
- 删除播放器首页新增的 HISTORY/library 数据读取、续播 Hero、最近播放、导入卡、分格面板、拖拽覆盖层和 scoped 首页 CSS。
- 恢复 Web 原有侧栏与标题栏结构，不保留本变更新增的 Web 设置按钮和单列布局。
- 影视库的稳定 hash → HISTORY.path 历史加载链路继续保留，未回退或改写转码相关边界。
- 定向 Vitest：2 个测试文件，共 19 项通过；`pnpm typecheck` 与本次文件定向 ESLint 通过。
- Electron 原版空态、Web 浅色和 Web 夜间主题均完成真实运行时取证；验收后恢复系统主题。

## 原版空态上的通用拖拽反馈

- 新增共享 `VideoDropZone`，统一处理文件拖入深度、子元素间移动、离开、释放、禁用和卸载清理，并只把第一个文件交给页面业务层。
- 播放器保留原版常驻图标与“点击或拖拽动漫到此处播放”；只有文件实际拖入时，才显示轻边框、浅底色和“释放以打开视频”。
- 影视库复用同一拖拽提示；释放有效 MP4/MKV 后进入播放器并沿用现有加载流程，详情弹层打开时禁用页面拖入。
- 定向 Vitest：3 个测试文件，共 22 项通过；`pnpm typecheck` 与本次文件定向 ESLint 通过。环境仍提示当前 Node 22，项目声明 Node 24。
- 在隔离的真实 Electron 开发窗口中，通过原生 `DragEvent`/`DataTransfer` 分别触发播放器和影视库拖入状态，并保存第 5 轮截图证据。

## 拖拽提示文案收敛

- 通用拖拽覆盖层移除“支持 MP4、MKV”，播放器与影视库只保留下载图标和“释放以打开视频”。
- 定向 Vitest：1 个测试文件，共 15 项通过；`pnpm typecheck` 与本次文件定向 ESLint 通过。环境仍提示当前 Node 22，项目声明 Node 24。
