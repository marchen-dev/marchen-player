# 第 6 轮验收报告

本轮按反馈进一步精简拖拽覆盖层，移除“支持 MP4、MKV”，只保留下载图标和“释放以打开视频”。

## 本轮内容

- 播放器拖入状态不再显示格式说明。
- 影视库通过共享组件同步生效，不保留重复文案。
- MP4/MKV 的实际格式校验、文件选择范围与错误反馈保持不变。

## 取证说明

- 使用隔离的真实 Electron 开发窗口，通过原生 `DragEvent` 与 `DataTransfer` 分别触发播放器和影视库拖入状态。
- 两个页面的运行时文本均确认包含“释放以打开视频”，且不包含“支持 MP4、MKV”。
